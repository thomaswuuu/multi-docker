sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t thomaswuuu/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true thomaswuuu/react-test npm test

after_success:
  - docker build -t thomaswuuu/multi-client ./client
  - docker build -t thomaswuuu/multi-nginx ./nginx
  - docker build -t thomaswuuu/multi-server ./server
  - docker build -t thomaswuuu/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push thomaswuuu/multi-client
  - docker push thomaswuuu/multi-nginx
  - docker push thomaswuuu/multi-server
  - docker push thomaswuuu/multi-worker
